[project]
name = "isola-e2e-tests"
version = "0.1.0"
description = "End-to-end tests for the Isola sandbox platform"
requires-python = ">=3.10"
dependencies = [
    "isola",
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "pytest-timeout>=2.0",
]

[tool.uv.sources]
isola = { path = "../../sdks/python", editable = true }

[tool.pytest.ini_options]
asyncio_mode = "auto"
timeout = 120
markers = [
    "smoke: fast subset for quick validation",
]
