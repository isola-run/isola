# charts/

Helm charts for deploying Isola. The Helm chart is the **source of truth** for all deployment configuration.

## Contents

- `isola/` — Main Helm chart for the operator, api-gateway, and all supporting resources
- `localstack-values.yaml` — Values override for local S3 testing with LocalStack
