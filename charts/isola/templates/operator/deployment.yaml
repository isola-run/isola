apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "isola.operator.fullname" . }}
  labels:
    {{- include "isola.operator.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.operator.replicaCount }}
  selector:
    matchLabels:
      {{- include "isola.operator.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
        {{- with .Values.operator.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "isola.operator.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "isola.operator.serviceAccountName" . }}
      {{- include "isola.imagePullSecrets" . | nindent 6 }}
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 10
      containers:
      - name: manager
        image: {{ include "isola.operator.image" . }}
        imagePullPolicy: {{ .Values.operator.image.pullPolicy }}
        command:
        - /operator
        args:
        - --leader-elect
        - --health-probe-bind-address=:8081
        - --sidecar-image={{ include "isola.operator.sidecarImage" . }}
        {{- if .Values.operator.runtimeClassName }}
        - --runtime-class={{ .Values.operator.runtimeClassName }}
        {{- end }}
        {{- if .Values.operator.priorityClassName }}
        - --priority-class={{ .Values.operator.priorityClassName }}
        {{- end }}
        ports:
        - name: health
          containerPort: 8081
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /healthz
            port: health
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: health
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          {{- toYaml .Values.operator.resources | nindent 10 }}
        securityContext:
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
        env:
        {{- $bucketUrl := include "isola.operator.storageBucketUrl" . }}
        {{- if $bucketUrl }}
        - name: ISOLA_SNAPSHOT_BUCKET_URL
          value: {{ $bucketUrl | quote }}
        {{- $credentialSecretName := include "isola.operator.snapshotCredentialSecretName" . }}
        {{- if $credentialSecretName }}
        - name: ISOLA_SNAPSHOT_CREDENTIAL_SECRET
          value: {{ $credentialSecretName | quote }}
        {{- end }}
        {{- end }}
        - name: ISOLA_UPLOADER_IMAGE
          value: {{ include "isola.operator.uploaderImage" . | quote }}
        - name: ISOLA_SNAPSHOT_SERVICE_ACCOUNT
          value: {{ include "isola.operator.snapshotServiceAccountName" . | quote }}
        {{- $imagePullSecretNames := include "isola.imagePullSecretNames" . }}
        {{- if $imagePullSecretNames }}
        - name: ISOLA_IMAGE_PULL_SECRETS
          value: {{ $imagePullSecretNames | quote }}
        {{- end }}
      {{- with .Values.operator.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.operator.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.operator.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.operator.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
