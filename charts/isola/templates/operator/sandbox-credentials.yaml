{{/*
Storage credentials secret for rootfs snapshot jobs.
Created only when existingSecret is NOT set AND both accessKeyId/secretAccessKey are provided.
*/}}
{{- if eq (include "isola.operator.rootfssnapshotEnabled" .) "true" -}}
{{- $existingSecret := .Values.operator.sandboxRuntime.gvisor.rootfssnapshot.storage.credentials.existingSecret -}}
{{- $accessKeyId := .Values.operator.sandboxRuntime.gvisor.rootfssnapshot.storage.credentials.accessKeyId -}}
{{- $secretAccessKey := .Values.operator.sandboxRuntime.gvisor.rootfssnapshot.storage.credentials.secretAccessKey -}}
{{- $region := .Values.operator.sandboxRuntime.gvisor.rootfssnapshot.storage.credentials.region -}}
{{- if and (not $existingSecret) $accessKeyId $secretAccessKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "isola.operator.rootfssnapshotCredentialSecretName" . }}
  namespace: {{ include "isola.sandboxNamespace" . }}
  labels:
    {{- include "isola.operator.labels" . | nindent 4 }}
    app.kubernetes.io/component: rootfssnapshot
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: {{ $accessKeyId | quote }}
  AWS_SECRET_ACCESS_KEY: {{ $secretAccessKey | quote }}
  {{- if $region }}
  AWS_REGION: {{ $region | quote }}
  {{- end }}
{{- end }}
{{- end }}
