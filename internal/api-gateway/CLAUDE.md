# internal/api-gateway/

API gateway service implementation. All logic lives in `handlers/`.

## Testing

```bash
make test-gateway                # Run all gateway tests
make test-gateway FOCUS="Create" # Run focused tests
```
